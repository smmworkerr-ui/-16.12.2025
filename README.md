
# VK Messenger AI - Инструкция по запуску v2.0

Это руководство поможет вам шаг за шагом запустить приложение.

---
🛑 **ВАЖНО: НОВАЯ СТРУКТУРА**
---
Весь код бэкенда теперь находится в папке **`backend_python/`**.
Все файлы `.txt` внутри этой папки нужно переименовать в `.py` перед запуском.

---

## Шаг 1: Подготовка Бэкэнда

1.  Зайдите в папку `backend_python`.
2.  Переименуйте все файлы `*.py.txt` в `*.py` (уберите `.txt` в конце).
    *   `run.py.txt` -> `run.py`
    *   `backend_core.py.txt` -> `backend_core.py`
    *   и так далее...
3.  Откройте терминал в этой папке.
4.  Установите зависимости:
    ```bash
    pip install Flask requests python-dotenv Flask-Cors
    ```
5.  Запустите сервер:
    ```bash
    python run.py
    ```

**Ожидаемый вывод:**
```
[*] DATABASE PATH: .../backend_python/main.db
[+] DB Initialized...
SERVER RUNNING AT: http://127.0.0.1:5000
```

## Шаг 2: Запуск Фронтенда

1.  Вернитесь в корневую папку.
2.  Откройте `index.html` в браузере.

---

## 🚑 Если что-то пошло не так
Проверьте файл `logs/app.log` или вкладку "Логи" в приложении.
