
# ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА (HANDOVER)

## 🎯 Основные направления
1.  **Центр Управления (Right Column):** Мы перенесли все технические настройки, логи и статистику в правую колонку вкладки "Запуск". Левая колонка — только для контента.
2.  **Автономность:** Функции "Секретаря" (авто-прием заявок, чистка исходящих) перенесены из рассылки в **фоновый процесс** (Maintenance Worker). Управление — через "Карточку Здоровья" аккаунта.
3.  **Удобство редактора:** Внедрен Inline-редактор шаблонов (прямо в списке блоков). Формат: одно поле textarea, варианты разделяются символом `///`.

## 🛠 Реализованный Функционал
*   **Вкладка "Запуск" (CampaignTab):**
    *   [x] Разделение на Левую (Контент) и Правую (Пульт) колонки.
    *   [x] Перетаскиваемая граница (Resizable).
    *   [x] Локальный зум (Aa) для правой колонки.
    *   [x] Полетные данные: Grid-сетка (3 колонки), карточки с разделением HP/Risk.
    *   [x] Конструктор: Inline-редактор, создание групп на лету, предпросмотр.
*   **Вкладка "Аккаунты" (AccountsTab):**
    *   [x] Drawer (шторка) при клике на аккаунт.
    *   [x] В шторке: Real-time статистика ВК, настройки Авто-Секретаря, настройки Авто-Чистки.
*   **Бэкенд:**
    *   [x] `maintenance_worker`: Фоновый процесс для авто-приема друзей.
    *   [x] `check_history_exists`: Функция проверки диалогов.

## ⚠️ ИЗВЕСТНЫЕ ПРОБЛЕМЫ (FIX ME FIRST)
1.  **Ошибка 404 Analytics:** При запуске иногда вылетает 404 на `/api/analytics/vk-stats`. *Причина:* файл `backend_api_analytics.py` может не импортироваться в `run.py` или файл не создан. **Нужно проверить `run.py` и наличие файла.**
2.  **Сброс настроек в Drawer:** При открытии карточки аккаунта настройки (тумблеры) могут "скакать" или сбрасываться при фоновом обновлении списка аккаунтов. *Решение:* Убрать зависимость от `accounts` в `useEffect` инициализации формы.
3.  **Импорты Python:** Иногда теряется `from backend_tools import check_history_exists` в `backend_worker.py`.

## 🚀 План действий (Roadmap)
1.  **Fix Bugs:** Устранить проблему с импортом аналитики и сбросом формы настроек.
2.  **UI Polish:** Убедиться, что Inline-редактор не исчезает при сохранении (уже реализовано, проверить стабильность).
3.  **Refactoring:** Удалить остатки кода старых тем оформления (CSS variables).

**ВАЖНО:** Питоновские файлы отдавать ТОЛЬКО как `.txt`.
